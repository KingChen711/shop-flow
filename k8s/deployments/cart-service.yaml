apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: shopflow
  labels:
    app: cart-service
    app.kubernetes.io/name: cart-service
    app.kubernetes.io/part-of: shopflow-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
    spec:
      containers:
        - name: cart-service
          image: shopflow/cart-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50058
              name: grpc
          envFrom:
            - configMapRef:
                name: common-config
            - configMapRef:
                name: cart-service-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - 'node -e "require(''net'').connect(50058, ''localhost'').on(''error'', () => process.exit(1)).end()"'
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - 'node -e "require(''net'').connect(50058, ''localhost'').on(''error'', () => process.exit(1)).end()"'
            initialDelaySeconds: 30
            periodSeconds: 30
      restartPolicy: Always
